async function load(){const [h,m,c]=await Promise.all([fetch("/healthz").then(r=>r.json()),fetch("/v1/models").then(r=>r.json()),fetch("/connections").then(r=>r.json()).catch(()=>({connections:[]}))]);document.querySelector("#status").textContent=h.status||"unknown";const models=m.data||[];document.querySelector("#models").textContent=models.length;document.querySelector("#providers").textContent=new Set(models.map(x=>x.owned_by)).size;document.querySelector("#accounts").textContent=(c.connections||[]).length;document.querySelector("#list").innerHTML=models.length?models.map(x=>`<div class="row"><b>${escapeHtml(x.id)}</b><span class="muted">${escapeHtml(x.owned_by)}</span></div>`).join(""):'<div class="row muted">No registered models yet.</div>'}function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]))}document.querySelector("#refresh").onclick=load;load();